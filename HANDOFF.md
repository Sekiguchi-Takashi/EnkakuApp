# EnkakuApp 引き継ぎ (v1.1)

## これは何か
画面を専有しない、小さな独立キーボード（IME単体）。遠隔操作機能は含まない。
2つの入力方式を内蔵し、実行中に切り替えられる。

- スライド展開式（v1.1・既定）: 1行に並んだ行頭(あかさたなはまやらわ)を押すと、
  左にカタカナ・右にひらがなが展開し、横スライドで選ぶ。1行で済むため画面を覆わない
- フリック式（v1.0）: 従来のテンキー・フリック入力

## レイアウト切替
- スライド式で「固定」キーを長押し → フリック式へ
- フリック式で「▽」キーを長押し → スライド式へ
- 選択は SharedPreferences("enkaku","layout") に保存され次回も維持

## スライド展開式(v1.1)の操作
- 行頭をタップ → その行が展開（中央=行頭ひらがな、右=い/う/え/お、左=カタカナ）
- 押したまま横スライド → 指の位置の文字、離して確定
- 固定キー: タップでON/OFF。ONのときは行頭タップで展開が保持され、
  スライドせずタップで選べる（左手で固定、右手でタップ）。左端タップで取消
- 数: 0と5を目印に 0〜9 を横スライド選択
- ゛小: 直前1文字を濁点/半濁点/小書きへ巡回
- 記号: . @ / _ : - 、 。 ？ ！ 〜 を横スライド選択
- 空白 / 改行: 独立キー（改行は入力欄のアクションに追従）
- ▽: タップで引っ込め、長押しでフリックへ切替

## 意図的に未実装（相談済み）
- かな漢字変換なし（確定はかな直接）。将来 段階C まで見据える
- 予測変換（列0）なし。段階B(かな語予測)→段階C(漢字変換)で追加予定
- 縦置き専用の列レイアウトは v1.2

## 構成
app/src/main/java/com/appathy/enkaku/
  EnkakuIme.kt          IMEサービス。2レイアウト生成・切替(setInputView)・入力適用
  SlideKeyboardView.kt  スライド展開式。1行・行頭展開・横スライド・固定ボタン
  FlickKeyboardView.kt  フリック式(v1.0)。長押し▽でレイアウト切替
  KanaTables.kt         フリック表/濁点巡回/かな↔カナ/行テーブル/展開ストリップ
  MainActivity.kt       有効化と切替の誘導、試し打ち欄
res/xml/method.xml      IMEメタデータ（ja / en）

## ロードマップ
- v1.2: 縦置き列レイアウト（列1あかさたな/列2はまやらわ、右展開）、都度押し固定の作り込み
- v1.3: 段階B 予測（列0・かな語先頭予測・内蔵辞書）
- v2.0: 段階C かな漢字変換（辞書＋変換器）

## ビルド/配布
- deploy.sh に集約。pull --rebase とタグ発行を含む（恒久仕様準拠）
- ci/ と .github/workflows/release.yml は CatalogApp 注入のため削除しない
- タグ発行で Actions がビルド→Release→自作ストアに更新として出る
