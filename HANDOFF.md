# EnkakuApp 引き継ぎ (v1.0)

## これは何か
画面を専有しない、フリック入力の**小さな独立キーボード（IME単体）**。
タブレット標準キーボードが画面を覆って邪魔になる問題への回答として、
高さを抑えたコンパクトなフリックキーボードを提供する。遠隔操作機能は含まない。

## v1.0 でできること
- フリック入力（あ行〜、句読点）。中央タップ＋左/上/右/下フリックで5音
- モードキー（左上）で あ → ア → A → 1 を巡回
  - あ=ひらがな, ア=カタカナ, A=英字(フリック配列), 1=数字/記号
- 濁点/半濁点/小書きキー（中央グリッド左下「゛小」）: 直前1文字を巡回変換
  例: は→ば→ぱ→は、つ→っ→づ、あ→ぁ
- 削除（長押しで連続削除）、空白、改行（入力欄のアクションに追従）、カーソル左右
- 🌐キーで他のIMEへ切替、▽キーでキーボードを引っ込める
- 横持ちでも全画面入力にならない（onEvaluateFullscreenMode=false）
- 固定高さ 210dp のコンパクト設計

## v1.0 に無いもの（意図的）
- **かな漢字変換なし**。確定はかな直接入力。変換エンジンは将来課題
- 予測候補なし、フローティング配置なし

## 構成
```
app/src/main/java/com/appathy/enkaku/
  EnkakuIme.kt          IMEサービス。入力適用・濁点変換・改行アクション判定
  FlickKeyboardView.kt  描画とタッチ。5列×4行、フリック判定とポップアップ
  KanaTables.kt         フリック表・濁点巡回表・かな↔カナ・英数表
  MainActivity.kt       有効化と切替の誘導、試し打ち欄
res/xml/method.xml      IMEメタデータ（ja / en サブタイプ）
```

## 進化ロードマップ（相談済みの方向）
- v1.1: フローティング/ドラッグ移動、高さ調整、キー配色テーマ
- v1.2: 放射状フリック（中央1ボタンから全かな）、入力欄種別に応じた配列変化、
        予測チップ列（打たずに候補だけ流す形態）
- 将来: かな漢字変換（辞書＋変換器）。ここまで来ると単体キーボードとして完成

## ビルド/配布
- `deploy.sh` に集約。pull --rebase とタグ発行を含む（恒久仕様準拠）
- `ci/` と `.github/workflows/release.yml` は CatalogApp が API 経由で注入するため削除しない
- タグ発行で GitHub Actions がビルド→Release→自作ストアに更新として出る
